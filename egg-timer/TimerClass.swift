//
//  TimerClass.swift
//  egg-timer
//
//  Created by hostname on 11/14/16.
//  Copyright © 2016 notungood. All rights reserved.
//

import Foundation
/*
class TimerClass {
    
    var secondsRemaining = 210

    var countDownTimer = Timer.scheduledTimer(
        timeInterval: 1,
        target: _,
        /* From stack overflow: The target can be any object that implements the given method. Sometimes you may want a specific object (other than self, which it usually is) to do the task after the timer fires. */
        selector: #selector(TimerClass.oneSecondPassed),
        userInfo: nil,
        repeats: true
        )
    
    @objc func oneSecondPassed() {
        if secondsRemaining > 0 {
            secondsRemaining -= 1
        } else {
            secondsRemaining = 0
        }
    }
    
    func tenMoreSeconds() {
        secondsRemaining += 10
    }
    
    func tenFewerSeconds() {
        if secondsRemaining >= 10 {
            secondsRemaining -= 10
        } else {
            secondsRemaining = 0
        }
    }
    
    func resetSeconds () {
        secondsRemaining = 210
    }
    
}
 */

